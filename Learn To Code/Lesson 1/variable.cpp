#include <iostream>
using namespace std;

int main()
{
	int a = 10;
	/* assigning 10 to variable a (int or integer is the type of the variable,
	and a is the identifier of the variable)
	variable in c++ is case-sensitive, so a and A is different variable
	*/
	// variable CAN ONLY BE DEFINED ONCE
	cout << a;
	//prints variable a
	return 0; 
}
