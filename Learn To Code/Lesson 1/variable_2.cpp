#include <iostream>
using namespace std;

int main()
{
	int a,b,sum;
	// if there are 2 or more variables, we can define the type using commas
	a = 10;
	b = 20;
	sum = a+b;
	// now variable sum is 30
	cout << sum;
	// prints the value of sum
	return 0;
}
