#include <iostream>
using namespace std;

int main ()
{
	for (int a=1;a<=10;a++)
	// the first indicate the base operation and doesnt change
	// the second indicates the condition, if true it will continue, if false will break
	// the third indicates the increment to the base operation
	{
	cout << a << endl;
	}
	return 0;
}
