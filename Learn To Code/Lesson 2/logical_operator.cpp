#include <iostream>
using namespace std;

int main ()
{
	x&&X
	// && is the and logical operator (both have to be true for the expression to be true)
	x||x
	// || is the or logical operator (one of them have to be true for the expression to be true(
	!x
	// ! is the not logical operator
}
