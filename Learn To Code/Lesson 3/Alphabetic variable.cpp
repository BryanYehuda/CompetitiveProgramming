#include <iostream>
using namespace std;

int main ()
{
	string nama = "Bryan Yehuda Mannuel";
	// string can be used to write long sentences and is used with double quotation marks
	char a = 'a';
	// char can be used to write only 1 word and is used with single quotation mark
	bool online = true;
	bool offline = false;
	// bool can be use to define whether a statement is true or false, if bool is assigne to integer 0 is false and 1 is true
	// but if integer is assigned to bool 0 means false and any other non-zero means true
}
